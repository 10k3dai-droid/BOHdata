# asin-tracker crawler — Python dependencies
# Cài: pip install -r requirements.txt (khuyến nghị dùng virtualenv)

keepa>=1.3.15          # Library chính thức Keepa API (product, product_finder, best_sellers)
supabase>=2.4.0        # Python client cho Supabase (ghi asins/snapshots)
python-dotenv>=1.0.0   # Load biến môi trường từ .env
